/*
let mongoose = require('mongoose');

let QuoteSchem = new mongoose.Schema({
    text: {required: true, type: String},
    _author: {type: Schema.Types.ObjectId, ref: 'Author'}
}, {timestamps: true});

module.exports = mongoose.model('Quote', QuoteSchema);
*/
